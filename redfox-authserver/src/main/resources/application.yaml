server:
  port: 8483

spring:
  application:
    name: redfox-authserver
  main:
    banner-mode: off
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5432/redfox
    username: postgres
    password: postgres
  validation:
    method:
      adapt-constraint-violations: true
  security:
    oauth2:
      authorizationserver:
        issuer: ${REDFOX_AUTHSERVER_ISSUER:http://localhost:8483}
        client:
          webapp-client:
            registration:
              client-id: webapp-client
              client-secret: "{bcrypt}$2b$12$z2I9UU/FZuhBaZzgegtUqePMo/Z1HetY8SlGzVwYLaeUeowvu8AI2"
              client-authentication-methods:
                - client_secret_basic
              authorization-grant-types:
                - authorization_code
                - refresh_token
              redirect-uris:
                - http://localhost:8480/oauth2/callback
              scopes:
                - openid
            token:
              access-token-time-to-live: 30m
              refresh-token-time-to-live: 8h
              reuse-refresh-tokens: false

management:
  endpoints:
    web:
      exposure:
        include: "health,metrics,prometheus"
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true

problem4j:
  detail-format: lowercase

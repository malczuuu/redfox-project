# RedFox BOM

Central Bill of Materials (BOM) for dependency version management in the RedFox project.

## Key Features

- Manages all dependency versions in one place.
- Used as a `platform` in all modules.
